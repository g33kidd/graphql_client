fragment PostFragment on Post {
  id,
  title,
  content
}

mutation AddPost($title: String, $body: String) {
  post: addPost(title: $title, body: $body) {
    ...PostFragment
  }
}

query Posts {
  posts {
    ...PostFragment
  }
}